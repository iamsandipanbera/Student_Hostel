<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_mess_logo"
        android:enableOnBackInvokedCallback="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SantiniketanMess"
        tools:targetApi="31">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SantiniketanMess.NoActionBar" />

        <activity android:name=".MainActivity" />
        <activity android:name=".AddDepositActivity" />
        <activity android:name=".AddMemberActivity" />
        <activity android:name=".CalculationActivity" />
        <activity android:name=".CalculationSummaryActivity" />
        <activity android:name=".MealCalculationActivity" />
        <activity android:name=".MonthlyCalculationActivity" />
        <activity android:name=".MonthlyComparisonActivity" />
        <activity android:name=".DepositHistoryActivity" />
        <activity android:name=".GoogleSignInActivity" />
        <activity android:name=".MemberDetailsActivity" />
        <activity android:name=".MemberSummaryActivity" />
        <activity android:name=".MembersListActivity" />
        <activity android:name=".MessActivity" />
        <activity android:name=".MonthlyExpensesActivity" />
        <activity android:name=".MemberDepositHistoryActivity" />
        <activity android:name=".Developer" />


    </application>

</manifest>